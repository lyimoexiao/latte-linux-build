name: repro-matrix
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { a: 1 }
          - { a: 2 }
          - { a: 3 }
    steps:
      - run: echo "static matrix a=${{ matrix.a }}"
