# Desktop 版增量包（XFCE 轻量桌面）
# 桌面核心
xfce4
xfce4-terminal
xfce4-power-manager
xfce4-screenshooter
xfce4-notifyd
lightdm
lightdm-gtk-greeter

# 网络（network-manager/wpasupplicant 已在 base-common）
bluez
blueman

# 音频（新发行版默认 PipeWire）
pipewire-pulse
alsa-ucm-conf
pavucontrol

# 触屏 / 输入
xserver-xorg-input-libinput
xserver-xorg-input-evdev
onboard

# 字体
fonts-dejavu-core
fonts-liberation

# 视频硬解（i965 VA-API，Cherry Trail/Gen8 唯一可用的 VA-API 驱动）
# 注意：i965 驱动上游已停止开发，libva 版本过新时可能失效（见 linux_latte/fix_file/README.md）
i965-va-driver
