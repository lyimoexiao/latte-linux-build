# Desktop 版增量包（XFCE 轻量桌面）
# 桌面核心
xfce4
xfce4-terminal
xfce4-power-manager
xfce4-screenshooter
xfce4-notifyd
lightdm
lightdm-gtk-greeter

# 网络（network-manager/wpasupplicant 已在 base-common）
bluez
blueman

# 音频（新发行版默认 PipeWire；wireplumber 是会话管理器，pipewire-pulse 只
# 把它列为 Recommends，--no-install-recommends 下必须显式安装，否则无声）
pipewire-pulse
pipewire-bin
wireplumber
alsa-utils
alsa-ucm-conf
pavucontrol
pulseaudio-utils

# 触屏 / 输入
xserver-xorg-input-libinput
xserver-xorg-input-evdev
onboard

# 字体
fonts-dejavu-core
fonts-liberation

# 视频硬解（i965 VA-API，Cherry Trail/Gen8 唯一可用的 VA-API 驱动）
# 注意：i965 驱动上游已停止开发，libva 版本过新时可能失效（见 linux_latte/fix_file/README.md）
i965-va-driver

# 相机（Cherry Trail atomisp + OV5693 主线驱动，内核已启用；ISP 固件 shisp_2401a0_v21.bin
# 已在 stage-2 部署。cam 命令来自 libcamera-tools，libcamera-v4l2 让 v4l2 应用可见）
libcamera-tools
libcamera-v4l2
